# SmartClip 弹框"闪一下"审查报告

> 审查对象：`H:\steward`（Qt 6.11.2 / MSVC2022 / C++ + QML）
> 审查标准：对话里那份"弹框显示策略"清单（显示前完成创建/尺寸/样式/数据，显示后只做一次 show/open；预创建；尺寸不许跳变；建议做统一 DialogManager）
> 方法：读源码（`src/`、`qml/`、`Main.qml`）+ 用本项目自己的 `qml.exe` 量具实测 + 盘点自检覆盖
> 第 1~6 节是**只读审查**的结论（当时未改任何源码）；第 0.2 节与第 7 节是之后动手做的
> "第 7 节第 1 步"（加通用自检 + 修红），改动都在第 0.2 节里逐条列着。

---

## 0. 结论

**符合，而且总体上比清单更严。** 清单里那 10 条，这个项目 7 条已经完全落地、3 条部分落地；
清单之外的 6 件事（DWM 转场、幕外预热、强制同步渲染、分窗口避免挪窗、show 前排空输入、3ms 采样自检）
是这个项目自己踩出来的，清单里没有，但方向完全一致。

真正还踩在"打开之后再动窗口 / 显示之后再改"这条红线上的，只有 **4 处**；
其中 **2 处**是用户点得到的（翻译卡片 / 便签上的"置顶"开关），**2 处**是已有注释说明的取舍。

**第 7 节第 1 步做完之后的账**（详见第 0.2 节）：那条红线上的 4 处里，
**P0-3（下拉菜单挪窗）、P0-4（便签面板改几何）已经修掉并被自检钉住**；
**P0-2（AskCard 打开后对中）实测不成立**（采样器看不到任何变化，撤回）；
**P0-1（置顶开关重建原生窗不推帧）还在** —— 它属于第 2 步，不在这一步的范围里。

它不是"每个弹框各打补丁"的状态 —— 全项目 **零 Loader、零 createObject**，所有弹窗都在
`Main.qml` 里静态建好；缺的只是**一个统一入口**（下面第 6 节）。

---

## 0.1 基线：先跑一遍 `--self-test`（改动之前的对照）

**命令**（和 Qt Creator 一样的跑法：最新的 exe + cwd = build 目录 + Qt 的 bin 进 PATH）

```
cd build\Desktop_Qt_6_11_2_MSVC2022_64bit_Debug
$env:PATH = 'D:\Program\Qt\6.11.2\msvc2022_64\bin;' + $env:PATH   # exe 旁边没部署 Qt 的 DLL，得让 Qt 的 bin 进 PATH
.\SmartClip.exe --self-test > selftest-baseline.txt 2>&1
```

**结果**

| 项 | 值 |
|---|---|
| exe | `build\Desktop_Qt_6_11_2_MSVC2022_64bit_Debug\SmartClip.exe`（2026-09-16 13:27:15，比最新源码 13:09:46 新） |
| 退出码 | **0** |
| 耗时 | 36.7 s（自带 60 s 看门狗，`src/main.cpp:838-841`） |
| 结果 | **通过 774 项，失败 0 项** |
| 便签那一节 | 通过 190 项，失败 0 项 |
| 翻译那一节 | 通过 70 项，失败 0 项 |
| 原始输出 | `build\Desktop_Qt_6_11_2_MSVC2022_64bit_Debug\selftest-baseline.txt`（820 行） |

与"闪"直接相关的那几条，基线里都是绿的：

```
ok  截图：选区窗口启动时已在幕外画过一帧（第一次抓屏不带空窗帧）
ok  截图：刚复位时先把选区边框压住（取消时不会闪一下全屏边框）
ok  截图：窗口稳住之后（settleOverlay）选区边框才出来
ok  截图：收工之后选区窗口收起来、状态复位（窗口留着复用，见 Screenshot::prewarm）
ok  窗口：退出问句是非模态的（不会把程序挡住）
ok  窗口：问句是一块小卡片，没有铺满整窗的遮罩
ok  窗口：问句收得掉（不留残窗）
ok  菜单：展开子面板时主栏窗口一动不动（不闪、不伸缩）   [208x353@1500 -> 208x353@1500]
ok  菜单：鼠标经过那条缝时子面板不会闪掉   [flyout=opacity]
ok  标签条：窗口 = 卡片 + 左边那一条（卡片位置尺寸都没变）   [窗口 364 / 卡片 330 / 标签条 34]
```

**⚠️ 关键：这份基线全绿，并不代表第 3 节那些毛病不存在。** 两者不矛盾 —— 基线绿是因为
**那几条自检根本没有覆盖**第 3 节列的问题。对照如下：

| 第 3 节的毛病 | 基线里有没有对应的检查 | 说明 |
|---|---|---|
| P0-1 改置顶标志重建原生窗、没推帧 | **没有** | 自检只断言 flags 的**值**（`窗口：无边框 + 置顶（钉在桌面上）`、`SelfTestNotes.cpp:956`），从没断言"跨过这次 flags 变更时有没有露出一帧空窗" |
| P0-2 AskCard 打开后重新对中 | **没有** | 问句那两条只查"非模态 / 不是满窗遮罩 / 收得掉"，不查位置在 open 之后有没有动 |
| P0-3 DropdownMenu 子菜单移动已打开的弹窗 | **没有** | 唯一的"不许动"检查钉的是**便签菜单**（`菜单：展开子面板时主栏窗口一动不动`），不是 `DropdownMenu` |
| P0-4 NoteMenu flyout 已显示下改几何 | **没有** | 同上：主栏窗口不动被钉住了，"飞出面板在切 kind 时改几何"没人管 |
| P1-5 DocCard 异步推位置 + 高度反推 y | **部分** | 有"卡片位置被推给界面了 / 算式对"（`SelfTestDoc.cpp:283,293`），但只对**算式**，不对"可见之后位置有没有变" |
| P1-6 CheckCard 先 show 再填数据 | **没有** | `grep checkCard\|diffCard src/SelfTest*` 零命中 |
| P1-7 QInputDialog 的 `adjustSize()` 时机 | **没有** | 全项目没有一条检查涉及 QtWidgets 对话框的调用顺序 |

所以基线要这么用：**它是"我改完之后不许倒退"的下限，不是"现在已经没有闪的问题"的证明。**
改完第 7 节那几条之后，跑同一条命令要求仍然是 774/0（或把新加的自检算进去之后仍然失败 0）。

---

## 0.2 第 7 节第 1 步：做完了（新基线 784/0）

**做了什么**：加了一条通用规矩的自检 —— *"弹窗第一次露出来的那一帧，就必须是它最终的样子"* ——
然后按"红在哪就改哪"把红的两条修掉。

### 新增的量具（`src/SelfTest.cpp`，匿名命名空间里，和 `check()` 放一起）

```cpp
struct SurfaceShot { bool valid, visible; QRect geo; Qt::WindowFlags flags; };
SurfaceShot shootSurface(QQuickWindow *);            // 量的是**原生窗口**，不是 QML 属性
bool surfaceStaysPut(const std::function<QQuickWindow *()> &windowOf, int samples, QString *detail);
```

- 从"第一次可见"那一帧起连采 5~6 帧（每帧 `processEvents` + 6ms），几何或窗口标志有任何一帧和第一帧
  不一样就报红，detail 里给"第一帧 -> 变掉那一帧"。
- 采原生窗口的几何/标志：闪的是原生窗口（这个工程里"属性对、屏幕上不对"栽过不止一次）。
- `windowOf` 是个每次现问的函数：弹窗的原生窗口是 `open()` 那一刻才建/映射的，先问一次会拿到空指针。
- 一帧都没跟上（露得太晚）= 失败：那种情况下"没看见它变"没有意义。

### 新增的 10 条检查（774 → 784）

| 检查 | 结果 |
|---|---|
| `弹窗：退出问句露出来的第一帧就是最终样子（不在打开之后对中）` | ✅（**预期会红，实测绿**，见下） |
| `弹窗：设置面板露出来的第一帧就是最终样子` | ✅ |
| `弹窗：下拉菜单露出来的第一帧就是最终样子` | ✅ |
| `弹窗：展开子菜单时弹窗的左上角一动不动（只往下长）` | 先 ❌ 后 ✅ |
| `弹窗：展开子菜单没有走到"露着的时候挪位置"那条兜底` | 先 ❌ 后 ✅ |
| `弹窗：子菜单把窗口撑得比宿主还高（这一枪没打空）` | ✅ |
| `菜单：面板露着的时候能换成另一块（透明度）` | ✅ |
| `菜单：再换回「与…组合」那一栏` | ✅ |
| `菜单：换面板时，面板窗口从来没在露着的时候被改几何` | 先 ❌ 后 ✅ |
| `菜单：换面板走的是"先藏、再摆、再露"` | 先 ❌ 后 ✅ |

"这一枪没打空"是**故意**的：子菜单那一条得先把主窗口压到 900x420，才能逼出"菜单比宿主还高"那个情形
（正常 1460x900 里根本走不到那条路，量了也是空的）。压完在收尾处把主窗口几何还原。

### 先红在哪（改动前的实测）

```
FAIL  弹窗：展开子菜单时弹窗是"长"出来的，不是"跳"过去的   [494x763@(1610,818)（连采 6 帧没变）；露着的时候挪了 1 次]
FAIL  菜单：换面板时，面板窗口从来没在露着的时候被改几何   [露着改了几何 2 次（要 0）]
FAIL  菜单：换面板走的是"先藏、再摆、再露"   [重新映射 0 次（换之前 0）]
通过 781 项，失败 3 项
```

注意第一条的细节：**采样器说"连采 6 帧没变"，计数器说"挪了 1 次"** —— 两句话都对。
挪这一下和改几何是**同一个事件回合**里发生的，帧与帧之间根本看不到；只有拿"展开前 / 展开后"
两个点直接比才抓得住。所以这条检查最后改成了直接比原生窗口的左上角，计数器只当兜底。

### 改了什么（4 个源码文件 + 2 个自检文件）

1. **`qml/components/DropdownMenu.qml` —— 子菜单挪窗（P0-3）**
   新增 `worstExpandedHeight()` / `worstExpandedWidth()`：按"这一份菜单里最坏的那个子菜单"预留尺寸，
   `openFor` / `openAtPoint` 用它一次把位置夹死；`openSubmenu` 里那条夹取降级成兜底 + 违规计数 `openShifts`。
   **踩的坑**：预留算式的下限必须和兜底算式的下限**一模一样**（都是 `max(2, …)`）——
   第一版预留落在 y=8、兜底想要 y=2，展开子菜单时窗口照样挪 6px，自检立刻红了。
   **代价写在代码注释里**：宿主窗口矮的时候，菜单会一开始就摆到"最坏情况也装得下"的位置，
   而不是先挂在它那一栏下面、等展开子菜单再跳上去。
2. **`qml/notes/NoteMenu.qml` —— 面板窗口露着的时候改几何（P0-4）**
   换成**先藏、再摆、再露**（`flyoutPainted=false` → `visibility=Hidden` → 赋值几何 → `Windowed`），
   并加两个计数：`flyoutShifts`（违规兜底，不许涨）、`flyoutRemaps`（走了几次"重新映射"，必须涨）。
3. **`Main.qml`**：给 `ddMenu` / `settingsPanel` / `checkCard` / `diffCard` 加 `objectName`（自检按名字找原生窗），
   `uiState()` 里报出 `menuOpenShifts`。
4. **`src/StickyNotes.cpp`**：`menuState()` 里把上面两个计数报给便签那节自检。
5. **`src/SelfTest.cpp` / `src/SelfTestNotes.cpp`**：量具 + 10 条检查。

### 改动后的验证

| 项 | 基线 | 现在 |
|---|---|---|
| 退出码 | 0 | **0** |
| 总计 | 774 通过 / 0 失败 | **784 通过 / 0 失败** |
| 便签那一节 | 190 / 0 | **194 / 0**（+4） |
| 翻译那一节 | 70 / 0 | 70 / 0 |
| 耗时 | 36.7 s | 38.0 s |
| 新多出来的 QML 警告 | — | **没有** |
| 原始输出 | `selftest-baseline.txt` | `selftest-final.txt` |

（中间两次过程输出留在同目录：`selftest-stageA.txt`（只加量具/计数，红 3 条）、
`selftest-stageB.txt` / `selftest-stageB2.txt`（修的过程中）。）

### ⚠️ 一条被实测推翻的结论（P0-2）

审查时我判断 **AskCard 的 `onImplicitHeightChanged → centerOnParent()` 会闪**，
理由是"`Column`（Positioner）的 `implicitHeight` 要等一次布局，所以打开时用的是上一张卡片的高度"。
**这条检查现在是绿的**：采样器从"第一帧可见"起连采 6 帧，几何一模一样 —— 也就是说
"高度定下来 → 重新对中"这一步落在**窗口还不可见**的时候（Qt 先摆好内容再映射窗口）。
所以：

- 第 3 节 P0-2 那条**降级为"理论风险"**（它依赖 Qt"映射窗口"和"抛光布局"的先后，换平台/换版本有变数）；
- 代码里那个"开完再对一次中"我**没有动** —— 实测没闪，改动只会引入新风险；
- 但那条自检留着：哪天时序变了、它真在可见之后对中，自检会立刻红。

这也正是这一步的价值：**先让自检说话，再决定改不改。**

---

## 0.3 用户报的"最大化时白一闪"：查到了，也修了

**用户原话**："向主程序界面最大化切换，界面会出现闪动，还有白色的背影一闪。"

### 结论

主窗口原来是**半透明**窗口（`WA_TranslucentBackground`，四角靠透明露桌面）。
**每次最大化都有一帧窗口是空的** —— 新表面还没画上内容，而半透明窗口的"空"就是
**透过去看它底下的东西**。底下正好是个白底网页/文档时，用户看到的就是"白色的背影一闪"
（顺带说，"闪动"里也有一部分是同一原因：`Main.qml` 那次"压暗→回全亮"的淡入在**半透明**
窗口上降的是不透明度，等于让整块界面变半透明、把桌面透出来；不透明窗口上降不透明度
才是注释里写的那个"压暗"）。

**修法**：主窗口改成**不透明 + 深色底**（`src/main.cpp` 里 host 的那一段）。
四角的圆角不受影响 —— 真正裁圆角的是 `WindowHelper` 的**遮罩**（`setMask`），
自检里有一条专门钉"遮罩落上了、左上角那一像素真的被裁掉"。

### 证据链（每一步都是量出来的，不是猜的）

用的是自检里那条采样器：把取样条放在**主窗口自己那块矩形里**（切换成最大化之后这块地儿
照样还是窗口的），所以它变亮只有一个解释 —— 那一帧窗口里没画东西。抓屏一次几十毫秒，
于是取样条只取 800x400（几毫秒一张，贴着帧率采）。

| 量到的东西 | 数据 | 说明 |
|---|---|---|
| 基准（切换前） | 白 0 / 亮 31 | 窗口在前台、量到的确实是它（这条当"这次量得成"的门槛） |
| 最大化 | **白 ~19,000 点 / 亮 ~250，恰好 1 帧** | 取样条 96% 是纯白 |
| 还原 | 从来不白 | 只有最大化那一下会 |
| 把那一帧存图看 | **里面是浏览器里那个白底页面** | 所以白不是"哪儿画了白色"，是**窗口透过去了** |
| 窗口类背景刷 | 0（不是那个传说中的系统白刷子） | 排除"WM_ERASEBKGND 用 COLOR_WINDOW 擦底" |
| 关掉 DWM 窗口转场 | 还是白 | 排除"系统拿旧表面做过渡动画" |
| 改成不透明（深色底） | **白 0 帧** | 修法成立（连跑 2 次 + 最终验证，452~456 帧全干净） |
| 改回半透明做对照 | 白 1 帧（亮 249） | 因果确认 |

### 顺带清掉的一处浪费

`WindowHelper::eventFilter` 原来在 **Move 和 Resize 都调 `applyRoundedMask()`** ——
遮罩是窗口**自己**的形状，跟它在屏幕上的位置无关，移动时重设纯属白挨一次
`SetWindowRgn`（每次都让整块窗口重画）。现在：Move 不再重设；`applyRoundedMask()`
里也加了"形状没变就不重复 setMask"的缓存。

### 踩过的两个坑（留个记录）

1. **一次"328 帧全白"的假红**：窗口位置/可见性/样式全对（不透明、非分层、可见、露出），
   但取样条里一直是浏览器 —— 那是**别的窗口抢到了前台**，量到的不是我们这块窗。
   现在每一步之前都先 `raise/activateWindow`，并且**先确认这一步量的是深色界面**
   （亮 > 100 就记成"没量成"、不参与判定）。教训：**抓屏量东西，必须自带"量的是谁"的判据。**
2. `windows.h`（经 `rpcndr.h`）里 `small` 是给 MIDL 用的宏（等于 `char`），
   而 `SelfTest.cpp` 里有个变量就叫 `small` —— 不 `#undef small` 会报一串莫名其妙的语法错误。

### 状态

| 项 | 值 |
|---|---|
| 自检总数 | **787 通过 / 0 失败**（基线 774 → 第 1 步 +10 → 0.3 节 +2 → 0.4 节 +1） |
| 新增检查 | `窗口：圆角遮罩落上了（左上角那一像素真的被裁掉）`、`最大化/还原：不露白，而且最大化是"一步到位"（没有放大动画）`、`窗口：主窗口关掉了系统转场动画` |
| 改的文件 | `src/main.cpp`（不透明+深色底）、`src/WindowHelper.{h,cpp}`（遮罩缓存 / Move 不重设 / 关转场动画）、`src/DialogStyle.h`（抽出 `disableDwmTransitions`）、`src/SelfTest.cpp`（探针+3 条检查）、`Main.qml`（uiState + 注释） |
| 未处理（同族，可另开一步） | 便签 / 翻译卡片 / 贴图窗口也都是"无边框 + 半透明"，它们被**改尺寸**时同样可能空一帧；用户这次报的是主窗口，先只修主窗口 |

---

## 0.4 "窗口会变到右边，在放大" + 树和正文之间那条缝在闪

**用户原话**："最大化时，窗口会变到右边，在放大"、"树形菜单和右边内容区的间隙，还有闪动"。

### 量到的事实

| 量法 | 结果 | 说明 |
|---|---|---|
| 窗口自己的 Move / Resize **几何序列** | `3840x2112@0,0 -> 1460x900@1600,816 -> …` | 每次切换**只有一拍**，不是"先按新尺寸待在旧位置"那种两步几何 |
| 探针角（常规窗口之外、最大化之后是左上角橙色图标那一块）：从按下到被界面盖住 | **18~25ms** | 窗口矩形**立刻**到位，没有"矩形慢慢长大"的动画 |
| 界面露白 | 0 帧（沿用 0.3 节的探针） | 白闪已经不在了 |

所以"变到右边、还在放大"**不是**窗口矩形在动，也不是两步几何 —— 剩下的解释只有一个：
**系统给最大化放的那段转场动画**。Windows 上它是拿窗口**上一次那张画面**按新矩形缩放
一遍交出去（窗口的矩形早就是最终值了），所以从外面量"窗口在哪儿"量不出来，
眼睛看到的却是"整块界面从旧位置（屏幕右边）放大过来"。**那条缝也在被缩放的那张画面里**，
所以它跟着一起缩 —— 用户看到的"间隙在闪"就是这么来的。

### 修法：关掉这个窗口的系统转场动画

`DialogStyle.h` 里本来就有这一手（`applyDarkTitleBar` 给 QtWidgets 对话框关 DWM 淡入，
注释里还记着实测："框的像素要在约 150ms 里才爬满…用户看到的就是'弹框闪动'"）。
这次把它抽成 `disableDwmTransitions(QWidget*)`，`WindowHelper::attachWidget()` 里对主窗口
也调一次 —— 不提供任何信息的动画，不值得留着换一次闪动。

### ⚠️ 这一条我量不了，只能靠眼睛

**DWM 的转场动画在进程里看不见**（窗口矩形是最终值，动画在合成器那边），所以自检能钉的只有
"这个属性确实设上了"（`WindowHelper::transitionsDisabled` → `uiState` → 一条检查），
**钉不住"眼睛看到的那一下"**。这一条请用户自己在真机上确认。

### 找不到时要往哪儿查（留给下一轮）

1. 拖左边树和正文之间那条分隔线 / 收起展开树：正文是**原生子窗口**（QScintilla），
   它的几何由 C++ 按 QML 的几何算着摆（`EditorViewItem::syncGeometry`）——
   摆慢一拍，那条缝就会跟着抖。
2. 拖窗口边改大小（不是最大化）：同样是一次 resize，但走的是另一条路（没有 WM 的
   maximize/restore），能分清"是转场动画"还是"布局慢一拍"。

---

## 1. 逐条对照清单

| 清单条目 | 项目实现 | 证据 | 判定 |
|---|---|---|---|
| 显示前完成创建、尺寸、样式、数据 | 菜单 `openFor/openAtPoint`：内容 → 算 x/y → `open()`；`SettingsPanel.show`：`placeOverHost()` → `open()`；`CheckCard/DiffCard`：`placeInParent()` → `show()`；`palettePopup.openAt`：算尺寸位置 → `open()` | `DropdownMenu.qml:221-240,261-274`、`SettingsPanel.qml:242-250`、`Main.qml:1324-1327,1392-1395`、`StickyNoteWindow.qml:1028-1042` | ✅ |
| 不要 `show()` 之后再 `resize`/`setStyleSheet` | `src/` 里**没有任何** show 之后的 `setStyleSheet`；顶级窗口**没有** show 之后的 `resize()` | 三个 QInputDialog 的样式/`ensurePolished`/`adjustSize`/暗色标题栏全部在 `exec()` 之前：`EditorController.cpp:558-567,834-847,851-864` | ✅ |
| 窗口标志在 show 之前定好 | 主窗口/便签/贴图/翻译卡片的 flags 都写在构造初始化列表里；`applyDarkTitleBar` 用 `winId()` **主动先建句柄**，所以能安全地在 show 之前调 | `main.cpp:249-252`、`DialogStyle.h:69-98`、`StickyNotes.cpp:307-309`、`Translate.cpp:872-874` | ✅ |
| 预创建，不要每次 `new` | 全树唯一一处动态创建是自检探针里的一个 `Timer`；所有 Popup/Window 静态声明 | `CaptureOverlay.qml:1018`（唯一命中，非界面）；`grep Loader\|createComponent\|createObject\|incubate` 无其它命中 | ✅ |
| 尺寸不要随内容跳变 | 尺寸走 `implicit*` 绑定或写死常量；长菜单限高+内部滚动；便签菜单**整场窗口一动不动** | `DropdownMenu.qml:178-179`、`CheckCard.qml:41-42`（460x470）、`DiffCard.qml:43-44`（980x600）、`NoteMenu.qml:337-338` | ✅ |
| 数据先填再显示 | `DiffCard` 是最标准的样本：`Differ.compare()` → 摆位 → `show()` | `Main.qml:1392-1395` | ✅ |
| 统一 DialogManager | **没有**。`DialogStyle.h`/`LightMenu.h` 只是两个 inline 自由函数；三个 QInputDialog 各自重复同一段 6 行前奏 | `DialogStyle.h:29-98`、`EditorController.cpp:559-562 / 835-838 / 852-855` | ❌（见第 6 节） |
| 不要"小窗一闪再变正常" | 用 `Popup.Window` 而不是新建顶层对话框，从结构上绕开"系统先映射空窗、内容下一帧才画" | `AskCard.qml:15-33`、`WindowHelper.cpp:73-86`、`SelfTest.cpp:5230-5237`（"用户在闪一下上折腾了三轮"） | ✅（超出清单） |
| QML 别用 Loader 现建现开 | 零 Loader；第一次打开不比第二次慢 | 同上 | ✅ |

---

## 2. 比清单更进一步的地方（清单里没有，但更硬）

1. **关掉 DWM 的窗口淡入** —— 实测"框的像素要约 150ms 才爬满（蓝色图标计数 146→288），用户看到的就是弹框闪动"，用 `DWMWA_TRANSITIONS_FORCEDISABLED` 只关这一个窗口。`DialogStyle.h:90-98`
2. **幕外预热 + 强制同步渲染** —— 选区窗口启动时在屏幕外 `show()` → 画一帧 → 摆回主屏再画一帧 → 藏。`Screenshot.cpp:353-433`；"不预热的话，QML 解析 / 场景图初始化 / 4K 首帧全落在'主窗口已经藏了、选区窗口还没出来'那段空档里，屏幕上露的就是桌面"
3. **show 之前先排空输入队列** —— 同步渲染那几十毫秒里攒下的 Esc 会让窗口"露一下才关"；这里 `PeekMessage` 看队列，已经取消了就**根本不 show**，"窗口一帧都不露"。`Screenshot.cpp:484-532`
4. **多窗口切换：先亮新的、推一帧、再收旧的** —— 便签换标签时顺序反过来就会"点一下闪一下"（各自独立的原生窗口，系统不会替你合成一帧）。`StickyNotes.cpp:2415-2448`
5. **收了之后再藏，防止旧画面被合成** —— "收起时先把面板清掉、等两帧再藏窗口"，否则下次 show 会把**上次那张画面**先合成一下。`NoteMenu.qml:209-216`（`flyoutPainted` + `flyoutHideDelayMs = 32`）
6. **自检用 3ms 采样抓单帧闪现** —— "闪一下"这种一闪而过的残留只能这么采；自检还钉住了"展开子面板时主栏窗口一动不动（不闪、不伸缩）"（±0.5px）。`SelfTest.cpp:3089-3116`、`SelfTestNotes.cpp:521`

---

## 3. 仍然踩在红线上的地方（按严重度）

### P0-1 改置顶标志 = 拆窗重建，没推帧 —— 用户点得到
`src/Translate.cpp:995-1012`（翻译卡片的"置顶"）、`src/StickyNotes.cpp:442-460`（便签的置顶）：
```cpp
setWindowFlags(flags);   // Qt 会把原生窗口 hide 掉再重建
setGeometry(geo);        // 几何收好了（注释里有交代）
show();                  // 但没有强制同步渲染一帧
```
注释只说到了"几何要自己收好，不然会跳回左上角"，没管**那一帧空窗**。
项目自己已经有解：`grabFramebuffer() + repaint()`（`StickyNotes.cpp:2424-2425` 称它是"这个工程里强制同步渲染、把这一帧推出去的老配方"）。
**建议**：`hide()` → `setWindowFlags` → `setGeometry` → `show()` → `grabFramebuffer()+repaint()`；或干脆先 hide 再改，改完再 show。

### P0-2 AskCard 打开之后重新对中 —— **实测不成立，撤回**（见第 0.2 节末段）
`qml/components/AskCard.qml:86-90,130-133`：
```qml
onImplicitHeightChanged: { if (opened) centerOnParent() }
```
注释自己写着"高度要下一帧才算得准…所以开完再按定下来的高度对一次中"。
审查时我判断这条会闪（`Text.implicitHeight` 同拍就更新，但 `Column`（Positioner）的 `implicitHeight`
要等一次布局），**但第 7 节第 1 步加的那条采样自检是绿的** —— 采样器从"第一帧可见"起连采 6 帧，
几何一模一样，说明"高度定下来 → 重新对中"落在**窗口还不可见**的时候。
所以：**代码不动**（实测没闪，改了只会引入新风险），第 3 节这条降级为理论风险，自检留着当哨兵。

### P0-3 打开子菜单时移动"已经打开的"弹窗 —— **已修**（见第 0.2 节）
`qml/components/DropdownMenu.qml`：位置改成开之前按"最坏展开尺寸"一次夹死，
`openSubmenu` 里那条夹取降级成兜底 + 违规计数。同一个项目在 `NoteMenu.qml:186-207`
已经否掉过这条路，并且写了实测结论：
> 一步挪到位：Windows 先把窗口的**旧画面**按新位置合成出来，Qt 下一帧才画新内容 —— 中间那一帧看着就是"菜单闪一下"。

（`NoteMenu` 的解法是**拆成两块窗口**；`DropdownMenu` 这次没拆窗，走的是"预留尺寸、只长不挪"。）

### P0-4 便签菜单的飞出面板在"已显示"状态下改几何 —— **已修**（见第 0.2 节）
`qml/notes/NoteMenu.qml` 的 `syncFlyoutWindow()`：几何要变而面板正露着时，改成
**先藏 → 再摆 → 再露**，并加 `flyoutShifts`（不许涨）/ `flyoutRemaps`（必须涨）两个计数。

### P1-5 DocCard：位置由 C++ 异步推，高度变了再反推 y
`qml/components/DocCard.qml:128-144` + `src/main.cpp:667-677`：
C++ 每 60ms 按**估算高度 118** 推一个 y，卡片再按**实际高度**反推 y（`onHeightChanged`）。
实用上安全（定时器从启动就在跑，卡片必然晚于它出现），但"先按估算位置显示、再按实际高度上移"仍然是尺寸跳变。
**建议**：C++ 只推"右下角锚点"，卡片在可见之前先量好高度摆好（例如 `visible: shouldShow && placed`）。

### P1-6 CheckCard：先 `show()` 再填数据
`Main.qml:1324-1328`：`placeInParent()` → `show()` → `Check.check(...)`。
同一个 JS 调用里完成，帧还没渲染，所以**实际不闪**；但顺序和规范相反（规范是数据先、show 后），
而且 `Check.check()` 里还有异步问模型那一段。建议调成"先 `check` 再 `show`"，或先 `Check.clear()` 再 show。

### P1-7 三个 QInputDialog 的 `adjustSize()` 早了一步
`src/EditorController.cpp:561 / 837 / 854`：`adjustSize()` 在 `setLabelText` / `setTextValue` **之前**，
尺寸是按"空标签状态"算的（后面 Qt 会重排，且在 `exec()` 之前，所以不闪）。
**建议**：把 `adjustSize()` 挪到设置完 label/value 之后。顺带说，这三处 6 行前奏一字不差地重复，
正是"没有一个统一 helper"的直接证据 —— 改一次得改三处。

### P2-8 首帧是方角（有注释的取舍）
`src/main.cpp:680-688`：圆角遮罩在 `host.show()` **之后**才落一次（`attachWidget` 那次因为原生句柄还没建而失效，"实测四角只裁掉了一个"）。
**建议**：host.show() 之前主动 `host.winId()` 建一次句柄再 `refreshMask()` —— 项目在 `applyDarkTitleBar` 里已经用了这个手法。
同族：`WindowHelper.cpp:260-269`（`showMaximized()` 之后补一次 `setGeometry(avail)`）也是"显示后再摆"，
同样是"先算好再切"可以消掉的一帧。

### P2-9 卫生问题（顺手清掉）
- `src/StickyNotes.cpp:2505-2508` 还留着调试代码：每次组合刷新把每块便签**整窗 grab** 再存一张
  `H:/steward/build/chip-*.png`，注释写着"临时…（查完删）"。
- `src/EditorViewItem.cpp:3867` 的 `QPrintDialog` 没做深色处理（一致性问题，不是闪烁）。
- `qml/screenshot/CaptureOverlay.qml:658/665/850/1741` 用 `recognitionCard.visible = true` 赋值，
  会**打断** `CaptureOverlay.qml:1766` 那条声明式绑定（`overlayReady` 门控第一次之后就失效了）。未验证。
- `CheckCard.placeInParent()` / `DiffCard.placeInParent()` 把 QML 内容层坐标（1460x900）直接写进
  `Window.x/y`，而 `Window` 的 x/y 是**屏幕坐标**（`DocCard.qml:117-126` 把这个坑记得很清楚：
  "宿主摆在 (300,160) 时 QML 读出来 x=0"）。`src/` 里**没有任何自检**覆盖这两块卡片的位置
  （`grep checkCard|diffCard src/SelfTest*` 零命中），所以这条没人钉。**待验证**，别照我说的直接改。

---

## 4. 我实测排除掉的两条（不用白改）

量具 `build/popup-measure.qml`（`qml.exe` 跑，窗口幕外映射以获得渲染循环）：

```
== ListView 换 model 之后 contentHeight 何时准 ==
  before assign : contentHeight=0    height=8
  same tick     : contentHeight=208  height=216      ← 同拍就准
== 关着的 Popup 里的 ListView ==
  same tick     : contentHeight=208  popup.height=216 ← 关着的 Popup 里也一样
== AskCard 式（内容列宽由 Popup 给） ==
  same tick     : body.h 15 -> 45（Text 同拍准）  但 Column.implicitHeight 还是 40
  +250ms        : Column.implicitHeight 仍是 40    ← Positioner 要等一次布局
```

- **`TranslateCard.qml:654` / `RecognitionCard.qml:490` 的"height 绑 contentHeight"没问题**：
  ListView 的 `contentHeight` 在 model 赋值当拍就更新完了，所以 `openAt()` 里读到的 `height` 已经是终值。
  （我一开始把它列成风险，实测后撤回。）
- **`AskCard` 的"高度下一帧才准"成立**：`Text` 同拍准，但 `Column` 要等一次布局 —— 所以 P0-2 是真问题。

---

## 5. 自检覆盖度（这是这个项目最强的一块）

> 基线（第 0.1 节）**774 项全绿**，而当时第 3 节那些毛病它一条都抓不到 —— 这正是"全绿和
> '还有闪的问题'不冲突"的原因。**第 0.2 节已经把第 1 类空白补上了**：
> 现在有 6 条针对主界面弹窗的检查（问句 / 设置面板 / 下拉菜单各一条"第一帧可见即最终样子"
> 的采样，子菜单三条"只长不挪"）+ 4 条便签面板的（切面板不许原地改几何、必须走重新映射）。
> 剩下还没被钉住的列在下面。

- 断言 API：`check(bool, name, detail)`，`src/SelfTest.cpp:68`；跑法：`SmartClip.exe --self-test`
  （另有 `--note-test` / `--translate-test` / `--doc-test` / `--tool-test` / `--doc-e2e` / `--doc-queue`）。
- 约 **90 条**与"闪/几何/位置"相关的检查。最强的几条：
  - `截图：选区窗口启动时已在幕外画过一帧（第一次抓屏不带空窗帧）` `SelfTest.cpp:3076`
  - `截图：第一次抓屏、%1ms 时按 Esc 取消 -> 窗口一帧都没露`（0/5/15/25ms，3ms 采样）`SelfTest.cpp:3124`
  - `截图：窗口稳住之后（settleOverlay）选区边框才出来` `SelfTest.cpp:3280`
  - `菜单：展开子面板时主栏窗口一动不动（不闪、不伸缩）` `SelfTestNotes.cpp:521`
  - `菜单：鼠标经过那条缝时子面板不会闪掉` `SelfTestNotes.cpp:575`
  - `窗口：问句是一块小卡片，没有铺满整窗的遮罩` `SelfTest.cpp:5287`
- **还没被钉住的**：
  1. ✅ *（已补）* 弹窗"露出来之后再改几何" —— 见第 0.2 节的 10 条新检查；
  2. 懒建即显 —— 只有 `Screenshot::prewarm` 有预热与首帧不空窗的检查；
  3. show 之后再套样式 / 改 flags（P0-1 那条"置顶"就落在这个空白里）—— 完全没覆盖；
  4. 第一次 show 与第二次 show 的差别 —— 只有选区窗口有；
  5. DWM 空窗帧、时间预算、首帧非空的像素证据 —— 都没有；
  6. `CheckCard` / `DiffCard` / `DocCard` 的"第一帧可见就是最终样子" —— 前两个要真跑校验/对比、
     后一个要真排一份文档进去（见第 0.2 节里那段说明），它们的几何由内容或固定值决定。

---

## 6. 关于"统一弹框框架"

项目**没有** `DialogManager`，但已经有事实上的三件套：
`uiState()` 反射（`Main.qml:1922+`，弹窗状态/几何都能读）+ `check()` 自检 + 每个文件头的踩坑注释。
所以"统一"缺的**不是 API，是两样东西**：

1. **一个 prepare→show 的规范 + 一个自检**（收益最大）：
   加一条通用检查 —— 任何 `Popup`/`Window` 在 `open()`/`show()` 之后**采样 3 帧几何与 flags，要求不变**。
   把 P0-3 / P0-4 / P1-5 这类问题一次钉死，以后新加弹窗也跑同一条。
2. **一个 C++ 侧的小 helper**：把三个 QInputDialog 那 6 行前奏收成一个
   `prepareDialog(QInputDialog&) → exec()`，顺序错只可能错在一处。

至于"所有弹框继承同一个基类"：这个项目里每个弹框的坐标系坑都不一样
（`Popup.Window` 的 x/y 写不进去、`Window` 的 x/y 是屏幕坐标、宿主 QWidget 的 geometry 和 QML 的 x/y 不是一套），
强行统一基类的收益不如上面两条。**先做自检那条，收益/成本比最高。**

---

## 7. 建议的动手顺序

| 顺序 | 做什么 | 文件 | 状态 |
|---|---|---|---|
| 1 | 加"打开后几何/flags 不许变"的通用自检（先让它红，红在哪就改哪） | `src/SelfTest.cpp`、`src/SelfTestNotes.cpp`、`qml/components/DropdownMenu.qml`、`qml/notes/NoteMenu.qml`、`Main.qml`、`src/StickyNotes.cpp` | ✅ **做完**（784/0，见第 0.2 节） |
| ~~3~~ | ~~AskCard 去掉二次对中~~ | — | ❎ **撤销**：自检实测不闪（见第 0.2 节末段） |
| ~~4~~ | ~~DropdownMenu 子菜单挪窗~~ | — | ✅ **已在第 1 步里修掉**（不是拆窗，是"预留最坏尺寸、只长不挪"） |
| 2 | 置顶开关：`hide → setWindowFlags → setGeometry → show → grabFramebuffer+repaint` | `src/Translate.cpp:1008-1011`、`src/StickyNotes.cpp:456-459` | 待做（外加一条"跨 flags 变更不许露空窗帧"的自检） |
| ~~8~~ | ~~最大化时"白一闪"（半透明窗口空一帧）~~ | `src/main.cpp`、`src/WindowHelper.{h,cpp}` | ✅ **做完**（见第 0.3 节：不透明+深色底，连跑 3 次 452~456 帧全干净） |
| 9 | 同族的其它无边框半透明窗口（便签 / 翻译卡片 / 贴图）改尺寸时同样可能空一帧 | `src/StickyNotes.cpp`、`src/Translate.cpp`、`src/PinWindow.cpp` | 待做（先确认它们真会，再照主窗口这招改） |
| 5 | 三个 QInputDialog 抽一个 prepare helper，`adjustSize()` 挪到数据之后 | `src/EditorController.cpp` | 待做 |
| 6 | 圆角遮罩改到 `host.show()` 之前（先 `winId()` 建句柄） | `src/main.cpp:680-688` | 待做 |
| 7 | 清 `StickyNotes.cpp:2505-2508` 的调试存图 | `src/StickyNotes.cpp` | 待做 |

验证：改完跑 `SmartClip.exe --self-test`（返回 0 = 全过），注意它**会开选区窗口 / 弹卡片**
/ 最大化几次，屏幕上会动十来秒；不想被打扰就先跑 `--note-test` / `--tool-test`
（这两条不显示主窗口）。
**当前基线：786 通过 / 0 失败 / 退出码 0**（第 0.2 / 0.3 节）—— 改完之后要求不低于这个数；
再加一条检查就再加一个数，**不许拿"把检查删掉"来换绿**。
