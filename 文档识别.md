# 文档 / 图片识别：三个引擎怎么选

这份文档回答一个问题：**PaddleOCR-VL 1.6、Granite-Docling 258M、RapidOCR
（RapidDoc）三个，给 SmartClip 用，哪个最好？**

结论先给：**三个不是同一层的东西，不存在"谁最好"**。它们是三种不同的技术路线，
代价和收益差一个数量级。真正该做的是**按场景分工**，这也是本项目的做法 ——
识别引擎是**可插拔**的（换一条命令就换引擎），默认走最轻的，想上精度就自己换。

---

## 一句话结论

| 引擎 | 是什么 | 一句话 |
|---|---|---|
| **RapidDoc**（RapidOCR 家族的文档流水线） | 传统流水线：版面 + OCR + 表格 + 公式，全 ONNX | **默认就用它**。800MB、不用显卡、开箱离线，中文够用 |
| **PaddleOCR-VL 1.6** | 0.9B 的视觉大模型（VLM）+ 版面模型 | **中文 / 混合语言、版面复杂时选它**。精度最高，但要 1.8GB 权重 + 独显才舒服 |
| **Granite-Docling 258M** | 258M 的小 VLM | **英文材料、机器资源紧张时选它**。最省，但中文是实验性的，别用来认中文 |

**RapidOCR 本身不做版面、表格、公式、阅读顺序** —— 它只是"检测 + 识别文字行"
（也就是项目里"图上选字"已经在用的那个）。要做"整篇文档转 Markdown"，得用
同一家的 **RapidDoc**（`rapid-doc` 包），它把版面 / 表格 / 公式 / 阅读顺序都
串起来了。这一点是最容易搞错的，下面第 3 节展开。

---

## 1. 三者根本不是一类东西

| | RapidDoc | PaddleOCR-VL 1.6 | Granite-Docling 258M |
|---|---|---|---|
| 路线 | 传统 CV 流水线 | 视觉大模型（VLM） | 视觉大模型（VLM） |
| 参数量 | 多个小模型（检测/识别/版面/表格/公式） | 0.9B（VLM）+ 版面模型 | 258M |
| 要不要 OCR 那一步 | 要（内置 RapidOCR，PP-OCRv6） | 不要，整页"读"出来 | 不要，整页"读"出来 |
| 输出 | Markdown / JSON | Markdown / JSON / HTML / xlsx / docx | DocTags → Markdown |
| 表格 | 结构化成 HTML 表格 | 结构化成 HTML/xlsx | 结构化（FinTabNet TEDS 0.97） |
| 公式 | 转 LaTeX | 转 LaTeX | 转 LaTeX（F1 0.968） |
| 阅读顺序 | 版面模型给 | 版面模型给 | 模型自己出 |
| 手写体 | 弱 | 较好 | 不主打 |

一句话：**RapidDoc 是"工程流水线"，另外两个是"模型"**。VLM 那种路线把"看懂
版面"和"读字"合成一件事，所以在复杂版面上更稳；代价是要显卡、要几百 MB 到
1.8GB 的权重、还容易出"幻觉"（编出原文里没有的字）。

---

## 2. 性能、占用、开源：逐项对比

### 开源情况（三个都是宽松许可，商用都没问题）

| | 许可 | 权重许可 | 维护方 | 更新节奏 |
|---|---|---|---|---|
| RapidDoc / RapidOCR | Apache-2.0 | Apache-2.0 | RapidAI 社区（主导 SWHL） | 约每月 |
| PaddleOCR-VL 1.6 | Apache-2.0 | Apache-2.0 | 百度 PaddlePaddle 官方 | 官方长期维护 |
| Granite-Docling 258M | Apache-2.0 | Apache-2.0 | IBM 官方 | 随 Docling 走 |

三个都是**真开源**（代码 + 权重都是 Apache-2.0），没有"开放权重但禁止商用"
那种限制。这一点上打平，不构成选型依据。

### 占用资源（这一项差距最大）

| | 权重体积 | 运行时内存/显存 | 需要独显吗 | 首次要下载 |
|---|---|---|---|---|
| **RapidDoc** | 约 810MB（ONNX，随包内置） | CPU 推理，几百 MB 内存 | **不需要** | **不用**（随包自带） |
| **PaddleOCR-VL 1.6** | 约 1.83GB（bf16）+ 版面模型 | 建议 ≥8GB 显存（官方 FastDeploy 建议） | 有独显才舒服；CPU 能跑但官方说"仅供快速验证" | 要（首次自动下） |
| **Granite-Docling 258M** | 约 515MB（bf16） | bf16 约 1.2~1.5GB（社区估） | 不用，但 CPU 上慢（Docling 自己给的 CPU 数据约 1175 秒/页，不可用） | 要 |

RapidDoc 那 810MB 里有一半是 `.safetensors` 副本（同一套 PP-OCRv6 权重存了
ONNX 和 safetensors 两份），实际跑只加载 ONNX 那份。

### 性能与精度

**精度**（第三方榜单，注意口径不同，别直接横比）：

| | OmniDocBench v1.6 |
|---|---|
| PaddleOCR-VL 1.6 | **96.33%**（官方）／96.34%（Roboflow） |
| Granite-Docling 258M | **没有官方成绩** —— IBM 只放了自家 docling-eval 的数据，Spheron 那边这一栏是 N/A。**任何给它标 OmniDocBench 分数的说法都不可信** |
| RapidDoc 流水线 | 有第三方给 90.16%，与专用 VLM 的 94~96% 差距主要**在表格**上 |

同榜单里 MinerU2.5-Pro 95.75%、GLM-OCR 0.9B 95.22%、Qwen3-VL-235B 89.78% ——
也就是说 PaddleOCR-VL 的 0.9B 在这项基准上赢过 235B 的通用模型。

**速度**（本机实测，RTX 4080 SUPER 16GB / i9 级 CPU；同一份 1 页 PDF，带文字层）：

| 场景 | 实测 |
|---|---|
| **RapidDoc**（CPU，ONNX） | **约 10 秒** |
| **PaddleOCR-VL 1.6**（GPU，模型已加载） | **约 9 秒** |
| RapidDoc，**图片 / 扫描件**（CPU 跑 OCR） | 约 20 秒 |
| RapidDoc，首次跑（加载模型 + ONNX 图优化） | 约 5 分钟（**一次性**，不是每页） |
| PaddleOCR-VL，首次跑 | 要下 1.8GB 权重（一次性） |

单页上两者差不多快，但**规模上差距会拉开**：RapidDoc 是 CPU 流水线，加页数基本
线性；PaddleOCR-VL 在 GPU 上还有批量空间（`pages_per_batch`）。所以文档越厚，
PaddleOCR-VL 的 GPU 优势越明显。

> 顺带一个实测到的注意点：PaddlePaddle 官方 cu126 包跟驱动带的 cuDNN 版本
> 可能对不上（本机报 *compiled with CUDNN 9.9, but CUDNN version in your
> machine is 9.5*）。它只是 **warning**，跑起来没问题，但值得知道。

### 质量差异（同一份 PDF，两个引擎的输出对比）

同一份测试文档，两边都跑通，但**结构还原的细度不一样**：

| | RapidDoc | PaddleOCR-VL 1.6 |
|---|---|---|
| 标题层级 | 全判成 `#`（一级） | **`##` / `###` 层级正确** |
| 表格 | 干净的 `<table>`（`<th>` 表头） | `<table border=1 style=…>`（带行内样式） |
| 公式 | `$E = m c ^ { \wedge } 2$`（`^` 被当字面量，**错**） | `$ E = mc^2 $`（**对**） |

也就是说 PaddleOCR-VL 不只是"分数高一点"，在公式和标题层级上是**看得见的更准**。
这也解释了为什么它值得作为"精度档"留着。

### 部署在 Windows 上的坑（这项 PaddleOCR-VL 明显吃亏）

| | Windows 原生 | 备注 |
|---|---|---|
| RapidDoc | **完全支持** | 纯 ONNX，`pip install rapid-doc` 装完就能跑，无 torch |
| PaddleOCR-VL 1.6 | 支持，但**加速后端要 Docker** | vLLM / SGLang / FastDeploy **在 Windows 上跑不了**，官方只给 Docker/Linux。Windows 原生只能走 PaddlePaddle 或 Transformers 后端（慢一些，但可用） |
| Granite-Docling | 支持（`docling` + transformers） | 要装 torch（额外 2GB+） |

PaddleOCR-VL 还有几条已知问题值得知道：**必须跑完整流水线**（官方明确警告
单独跑 VLM 会"大量幻觉"）、HF 上有"重复输出导致不可用"的讨论串、
Transformers 版本兼容性断过几次（4.57.6 / 5.3.0 都有问题）。

---

## 3. 容易搞错的三个点

**（1）RapidOCR ≠ RapidDoc。** `rapidocr` 只做"检测 + 方向分类 + 识别"，**没有**
版面分析、表格结构、公式、阅读顺序。这些在另外的包里：`rapid-layout`、
`rapid-table`，而把整条串成"PDF → Markdown"的是 **`rapid-doc`**。本项目用的是
`rapid-doc`。

**（2）`rapidocr-onnxruntime` / `rapidocr-openvino` / `rapidocr-paddle` 全都
废弃了。** 2025 年 1 月之后不再维护，能力并进了 `rapidocr`（引擎用
`EngineType` 选）。现在正确的装法是 `pip install rapidocr onnxruntime`。
网上大量教程还在教老的包名。

**（3）`docling-ibm-models` 不是跑 Granite-Docling 那个模型的包。** 那个包只有
TableFormer / 版面，VLM 本体要走 `transformers` / `vllm` / `mlx-vlm` / ONNX。

---

## 4. 本项目怎么做的：可插拔，默认最轻

架构上和"图上选字"（`src/PinOcr.h`）是同一套 —— **不把模型做进进程**，而是跑
本机一条命令：

```
<整条命令…>  <输入文件>  <结果.json>
```

两个路径接在最后（被 `python 脚本.py` 的参数顺序逼的）。脚本往结果文件里写：

```json
{"markdown": "# 标题\n\n正文…", "images": {"fig1.png": "<base64>"}, "pages": 12}
```

**换引擎 = 换一条命令**，C++ 一行都不用改。三个 runner 都随包放在
`resources/scripts/` 下：

| 脚本 | 对应引擎 | 依赖 |
|---|---|---|
| `doc_runner_rapid.py` | **RapidDoc（默认）** | `pip install rapid-doc` |
| `doc_runner_paddleocr_vl.py` | PaddleOCR-VL 1.6 | `paddlepaddle-gpu` + `paddleocr[doc-parser]` |
| `doc_runner_granite.py` | Granite-Docling 258M | `docling` + `torch` + `transformers` |
| `doc_runner_common.py` | 三者共用的**契约**（参数怎么读、结果怎么写） | —— |

### 给这台机器（RTX 4080 SUPER 16GB）的建议

1. **日常就用默认的 RapidDoc** —— 中文材料够用、不用显卡、装完离线可用。
2. **遇到版面复杂 / 表格多 / 中英混排的材料**，切到 **PaddleOCR-VL 1.6**。
   16GB 显存绰绰有余（官方建议才 8GB），CC 8.9 也不在"T4 那种会 OOM"的
   范围内；注意别去装 vLLM（Windows 上要 Docker），用 PaddlePaddle 原生后端。
3. **Granite-Docling 不用装** —— 除非以后要处理英文材料且不想占显存。中文
   材料上它官方都标"实验性、未验证"。

### 一个必须注意的坑：Python 解释器

识别包通常装在**虚拟环境**里，而 PATH 里的 `python` 往往是系统那个 —— 结果是
装了却报 `No module named 'rapid_doc'`，而且看不出原因。所以设置面板里
（设置 → 识别）有**单独一项「Python」**，指到那个 venv 的 `python.exe` 即可
（本机就是 `H:\steward\venv\Scripts\python.exe`）。指完之后，默认那条命令里的
解释器会自动跟着换。

---

## 5. 怎么用

把 PDF / 图片 / Word / Excel / PPT **拖进主窗口**，或者走
**「文件 → 识别文档…」**，或者点左边图标栏那个**取景框**。识别跑在后台，右下角
有小卡片显示进展；认完存成一份新笔记 ——

```
文档/SmartClip/剪贴板/2026-09-16/011744.md      笔记正文
文档/SmartClip/剪贴板/2026-09-16/assets/*.png   文档里的插图
```

（`文档/SmartClip` 是**保存位置**，可以在设置里改；`剪贴板` 是它下面固定的一层，
见下一节。）正文里图片用相对路径引用，整个目录搬走也不丢图。

设置面板 **设置 → 识别** 里可以：

- 换引擎（三个按钮，换的就是那条命令）
- 选档位：**快**（关掉表格和公式识别，一份 PDF 快好几倍）/ **均衡** / **最准**
- 指 Python 解释器
- 直接手写那条命令（想接自己的脚本、调更细的参数都行 —— 命令是唯一的接口）

更细的说明写在代码里：`src/DocConvert.h`（约定和解析）、`src/DocImport.h`
（队列、设置、落盘）、以及每个 runner 脚本自己的文件头。

### 自检怎么跑

识别这三条链路由三个自检入口钉着（都在 `Debug\SmartClip.exe` 上跑）：

```
SmartClip.exe --doc-test                                                  # 快，65 项，不跑脚本
SmartClip.exe --doc-e2e  --doc-python=H:\steward\venv\Scripts\python.exe  # 真跑脚本，8 项
SmartClip.exe --doc-queue --doc-python=H:\steward\venv\Scripts\python.exe # 走异步队列 + 位置推送，19 项
```

`--doc-python=` 是给自检用的（依赖装在 venv 里，而 PATH 里的 python 多半不是
那个），**它不写设置** —— 自检不该动用户配置。日常用不到：界面上那条路走的是
设置里配的解释器（设置 → 识别 → Python）。

还有个**看着对不对**的口子（位置 / 层级这种事只有眼睛说了算）：

```
SmartClip.exe --doc-demo=D:\某份.pdf     # 启动就排一份文件进去，窗口照常显示
```

### 一条踩过的坑：进度卡片的位置

卡片的位置栽了**三次**，每次都是"自检绿了但眼睛一看不对"，所以记在这儿：

1. **被编辑区盖住**：卡片原本是场景内的浮层（Rectangle），而编辑区是**原生
   QScintilla 子窗口**——原生子窗口永远画在 QQuickWidget 内容之上，**z 值改不了**。
   用户只看到从编辑区左边缘往左露出来的一小条。原因和 `DropdownMenu` /
   `SettingsPanel` 文件头里写的是同一件事。
2. **`Popup.Window` 的 x/y 写不进去**：改成独立原生窗口之后，先试了那两个组件用的
   `Popup.Window`，结果算好的坐标写下去、读回来是 `0,0`（连硬写 `111,222` 都留不住），
   卡片贴在左上角。菜单/面板是"跟着鼠标点弹出来"的，Popup 自带定位合适；卡片要
   "贴主窗口右下角、跟着走"，得自己管几何。
3. **`window.x` 不是宿主窗口的坐标**：换成普通 `Window` 之后，用
   `window.x + window.width - …` 算位置——**坐标系不对**。实测宿主 QWidget 摆在
   `(300,160)` 时，QML 那个 ApplicationWindow 读出来 `x=0`，于是卡片被算到屏幕中间去。
   而且**在 `reposition()` 里赋值会把绑定打断**，主窗口一移动就再没人重算，卡片钉在
   原地不动。

**最终做法**：位置**不由 QML 算**，由 C++ 推——只有 `main.cpp` 那边知道宿主 QWidget
的真实几何。一个 60ms 的定时器把"宿主右下角往里收一圈"的屏幕坐标推给 `Doc`
（`DocImport::publishCardGeometry`），QML 接住摆好（`DocCard.placeAt`）。

几个间距的**唯一真相在 `DocImport`**（`cardMargin` / `cardBottomGap` /
`cardWidth` / `cardHeightHint`），main.cpp 和 QML 都从那儿取 —— 以前散在 QML 和
main.cpp 两处，改一个忘一个就错位。当前值：

| | 值 | 说明 |
|---|---|---|
| 右边留白 | 16px | `cardMargin()` |
| 底边留白（可见） | 16px | `cardBottomGap() = 26 + 16`，26 是状态栏高度 |

底边那个 26 是状态栏（`StatusBar.qml` 的 `implicitHeight`），所以"可见留白"要拿
`cardBottomGap() - 26` 算。中间调过两次：一开始写 26（正好贴着状态栏上沿，看着很
挤），后来写 36（底下 10px，比右边窄、看着不齐），最后和右边统一成 16。

自检能验的是"位置确实被推过来了"和"那个算式对不对"（纯函数
`DocImport::expectedCardPos`）；**"卡片真的贴在右下角、主窗口移动时跟着走"需要
窗口真的显示出来**，所以由 `--doc-demo` 肉眼 + 截图确认。自检量不了这个 ——
它跑的时候主窗口是隐藏的，宿主几何和 QML 几何对不上，硬比就是自欺欺人（前两版
自检都这么"通过"过）。

---

## 6. 这份文档的来源

- 三个引擎的版本、许可、体积、基准分数：调研自各官方仓库 / 模型卡 / 文档，
  原文报告留在 `rapidocr-assessment.md`、`granite-docling-258m-report.md`。
- 速度数字、质量对比、参数名、错误路径：**本机实测**（RTX 4080 SUPER 16GB +
  `H:\steward\venv`）。三个 runner 都真跑过：RapidDoc 与 PaddleOCR-VL 在真
  PDF 上跑通并产出正确 Markdown；Granite-Docling 在本机没装依赖，验的是
  "给出人话错误"而不是崩。
- 端到端可用性：三个自检入口都跑过且全绿 ——

  | 入口 | 验什么 | 结果 |
  |---|---|---|
  | `--doc-test` | 解析 / 图片引用改写 / 命令拼法 / 落盘（不跑脚本） | 65 项全过 |
  | `--doc-e2e` | 真造 PDF → 真调脚本 → 落成笔记 | 8 项全过 |
  | `--doc-queue` | enqueue → 线程池 → 落笔记 + 位置推送 / 算式 | 19 项全过 |
  | `--self-test` | 全量回归（含便签 190 / 翻译 70） | 754 项全过 |

**哪些是"不确定"的，也标出来**：Granite-Docling 没有可信的 OmniDocBench 成绩；
PaddleOCR-VL 的官方吞吐量只给了一张图，第三方那个"约 1.22 页/秒（A100）"
没有找到原始出处；RapidDoc 的论文级精度数字没有看到官方发布，90.16% 那条
来自第三方横评。这些都不要当成硬数据用。
