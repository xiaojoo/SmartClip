# Ignore everything inside build_verify/
build_verify/
/build/
/.qtcreator
/.review-sc
/.idea
/build_mod
/cmake-build-debug
# Python 虚拟环境（图上选字那条路的 rapidocr、文档识别那条路的 rapid-doc /
# paddleocr 都装在这儿，不进仓库 —— 见「文档识别.md」里的装法）
/venv/
# 跑随包脚本时 Python 自己生成的字节码缓存（resources/scripts/ 下会冒出来）
__pycache__/
# 截图那条路的调试痕迹（Screenshot.cpp 自己写的调试文件，落在**工作目录**下，
# 也就是仓库根）。不该跟着提交 —— 它是用来查"取消截图时闪一下边框"的。
shot-trace.log
# 窗口变化日志（WindowHelper.cpp 写的：最大化 / 还原那一下窗口每一拍变成了什么样，
# 见 src/WindowHelper.h 里"窗口变化日志"那段）。同样落在工作目录下，同样不该提交。
window-trace.log
